# Worker-specific requirements
# Note: All dependencies are now consolidated in the root requirements.txt
# This file is kept for reference. Use: pip install -r ../../requirements.txt

# For backward compatibility, you can still install from this file
# but it's recommended to use the root requirements.txt instead
